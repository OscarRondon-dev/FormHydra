# Variables de entorno para FormHydra Backend
# Copia este archivo como .env y ajusta los valores

# Puerto del servidor (Railway lo asigna automáticamente)
PORT=3000

# Base de datos MongoDB
# Para Railway: usar ${{MongoDB.DATABASE_URL}}
# Para MongoDB Atlas: mongodb+srv://usuario:password@cluster.mongodb.net/formhydra
MONGODB_URI=mongodb://localhost:27017/formhydra

# Clave secreta para JWT (CAMBIAR EN PRODUCCIÓN)
JWT_SECRET=tu_clave_secreta_super_segura_cambiala_en_produccion

# Tiempo de expiración del token
JWT_EXPIRES_IN=24h

# Entorno
NODE_ENV=development

# URL del frontend (para CORS)
FRONTEND_URL=http://localhost:4200